name: Deploy slidev pages
on:
  push:
    branches:
      - main
      - test-ci
  workflow_dispatch:
jobs:
  build-pages:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        name: Install pnpm
        id: pnpm-install
        with:
          version: 10

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_ENV

      - name: Cache pnpm dependencies
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Install dependencies
        run: pnpm install

      - name: Build slides
        run: |
          mkdir -p dist
          for slide in $(ls slides); do
            pnpm --filter "./slides/$slide" run build --base "/$slide" -o "../../dist/$slide"

            # Create 404.html for redirection
            cat > "dist/$slide/404.html" << 'EOF'
              <!DOCTYPE html>
              <html lang="en">
              <head>
                <meta http-equiv="refresh" content="0; url='/$slide'" />
              </head>
              <body>
                <p>Redirecting to <a href="/$slide">/$slide</a>...</p>
              </body>
              </html>
            EOF
          done

      - name: Upload artifact for GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy-pages:
    needs: build-pages
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
